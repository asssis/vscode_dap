{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (Toy Ruby DAP)",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/example"
      ]
    }
  ]
}
